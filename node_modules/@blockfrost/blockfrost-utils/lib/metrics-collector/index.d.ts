/// <reference types="node" />
export declare class MetricsCollector {
    metrics: {};
    prefix: string;
    intervalId: NodeJS.Timer | undefined;
    getAdditionalMetrics: (() => Promise<Record<string, unknown>>) | undefined;
    constructor(interval: number, options: {
        prefix: string;
        getAdditionalMetrics?: () => Promise<Record<string, unknown>>;
    });
    private _getPrefixedKey;
    private _collect;
    startCollector: (interval: number) => Promise<void>;
    stopCollector: () => void;
    toPrometheus: () => string;
}
